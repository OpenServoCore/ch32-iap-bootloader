[package]
name = "tinyboot-macros"
version = "0.2.1"
edition.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
defmt = { workspace = true, optional = true }

[features]
defmt = ["dep:defmt"]
