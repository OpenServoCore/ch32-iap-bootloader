name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  libs-core:
    name: Libraries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: rustup toolchain install stable --profile minimal --component rustfmt,clippy
      - run: cargo fmt --all -- --check
      - run: cargo clippy -p tinyboot -p tinyboot-protocol --all-targets -- -D warnings
      - run: cargo build -p tinyboot -p tinyboot-protocol
      - run: cargo test -p tinyboot -p tinyboot-protocol
      - run: cargo doc -p tinyboot -p tinyboot-protocol --no-deps
      - run: cargo build -p tinyboot -p tinyboot-protocol --release

  cli:
    name: CLI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tinyboot-cli
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - run: rustup toolchain install stable --profile minimal --component rustfmt,clippy
      - run: cargo fmt --all -- --check
      - run: cargo clippy -- -D warnings
      - run: cargo build
      - run: cargo build --release

  ch32:
    name: CH32 (${{ matrix.chip }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        chip: [ch32v003f4p6, ch32v003a4m6, ch32v003f4u6, ch32v003j4m6]
    steps:
      - uses: actions/checkout@v4
      - name: Install toolchain
        run: rustup toolchain install nightly --profile minimal --component rust-src,rustfmt,clippy
      - name: Clippy libraries
        run: cargo +nightly clippy -p tinyboot-ch32-hal -p tinyboot-ch32-boot -p tinyboot-ch32-app --features ${{ matrix.chip }} --target examples/ch32/boot/riscv32ec-unknown-none-elf.json -Zbuild-std=core -Zjson-target-spec -- -D warnings
      - name: Build libraries
        run: cargo +nightly build -p tinyboot-ch32-hal -p tinyboot-ch32-boot -p tinyboot-ch32-app --features ${{ matrix.chip }} --target examples/ch32/boot/riscv32ec-unknown-none-elf.json -Zbuild-std=core -Zjson-target-spec --release
      - name: Doc libraries
        run: cargo +nightly doc -p tinyboot-ch32-hal -p tinyboot-ch32-boot -p tinyboot-ch32-app --features ${{ matrix.chip }} --target examples/ch32/boot/riscv32ec-unknown-none-elf.json -Zbuild-std=core -Zjson-target-spec --no-deps
      - name: Fmt boot
        run: cd examples/ch32/boot && cargo +nightly fmt --all -- --check
      - name: Clippy boot (system-flash)
        run: cd examples/ch32/boot && cargo +nightly clippy --features "${{ matrix.chip }},system-flash" -- -D warnings
      - name: Build boot (system-flash)
        run: cd examples/ch32/boot && cargo +nightly build --features "${{ matrix.chip }},system-flash" --release
      - name: Clippy boot (user-flash)
        run: cd examples/ch32/boot && cargo +nightly clippy --features "${{ matrix.chip }},user-flash" -- -D warnings
      - name: Build boot (user-flash)
        run: cd examples/ch32/boot && cargo +nightly build --features "${{ matrix.chip }},user-flash" --release
      - name: Fmt app
        run: cd examples/ch32/app && cargo +nightly fmt --all -- --check
      - name: Clippy app (system-flash)
        run: cd examples/ch32/app && cargo +nightly clippy --features "${{ matrix.chip }},system-flash" -- -D warnings
      - name: Build app (system-flash)
        run: cd examples/ch32/app && cargo +nightly build --features "${{ matrix.chip }},system-flash" --release
      - name: Clippy app (user-flash)
        run: cd examples/ch32/app && cargo +nightly clippy --features "${{ matrix.chip }},user-flash" -- -D warnings
      - name: Build app (user-flash)
        run: cd examples/ch32/app && cargo +nightly build --features "${{ matrix.chip }},user-flash" --release
